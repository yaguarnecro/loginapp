// es una clase de tyscript, para los campos del formulario,
// con instancias en registro components de este modelo, asociamos los datos al objeto.

export class UsuarioModel {
    email: string;
    password: string;
    nombre: string;
}
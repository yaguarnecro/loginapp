// es una clase de tyscript, para los campos del formulario,
// con instancias de este modelo, asociamos los datos al objeto.

export class usuarioModel{
    email: string;
    password: string;
    nombre: string;
}